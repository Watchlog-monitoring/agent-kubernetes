apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "watchlog-agent.fullname" . }}-test-connection"
  labels:
    {{- include "watchlog-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "watchlog-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
